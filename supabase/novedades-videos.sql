insert into storage.buckets(id,name,public,file_size_limit,allowed_mime_types) values ('novedades-videos','novedades-videos',true,52428800,array['video/mp4','video/webm']); create policy novedades_videos_read on storage.objects for select to anon,authenticated using(bucket_id='novedades-videos'); create policy novedades_videos_insert on storage.objects for insert to authenticated with check(bucket_id='novedades-videos' and (select public.is_admin_user())); create policy novedades_videos_delete on storage.objects for delete to authenticated using(bucket_id='novedades-videos' and (select public.is_admin_user()));